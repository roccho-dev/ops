# Project Workspace

## Project Source-only input rule

Project handoff source goes through Project Source.

## worker-readable proof

Upload visibility alone is not enough. The worker must read back `REQUEST.md`.

Thread roles use `role.chatgpt.thread` and canonical `threadFunction` names.
