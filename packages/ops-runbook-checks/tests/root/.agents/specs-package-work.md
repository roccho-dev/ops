# Specs Package Work

`spec.output.package = feat.input.package`.

The worker oracle is `package/package.json`.

Implementation starts only after the package is `implementation-ready`.
Missing contract state is `specs-contract-completion`.
