# Transport Contract

Project Source input uses `project-source-upload`. Thread attachment uses
`thread-file-upload` and is not Project Source proof. CDP readback is
`cdp-readback`.

`transport-sent` is only a send fact. `transport-read` is only readback.
Neither is semantic approval. Artifact names without hash and materialization
are `artifact-observed`.
