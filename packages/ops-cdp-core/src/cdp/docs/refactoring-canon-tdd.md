# Refactoring canon-tdd matrix

Scope: `cdp.zip` refactoring with no intended feature degradation.
Policy: operational `chromium-cdp-*` command names are kept inside `packages.<system>.cdp`; legacy dot-prefixed implementation files and legacy façade surfaces are not kept.

| # | area | target | refactoring content | canon-tdd | status |
|---:|---|---|---|---|---|
| 1 | IR gate | `test-chatgpt-ir.mjs` | Rebuilt the broken IR test file from the truncated `const pr` state into a full canonical IR contract test. | `node --check test-chatgpt-ir.mjs`; runtime gate: `qjs --std -m test-chatgpt-ir.mjs` | refactored |
| 2 | IR schema | `chatgpt/ir.mjs` | Promoted canonical thread IR from generic conversation naming to `CHATGPT_THREAD_IR_SCHEMA`. | grep gate: no `CHATGPT_IR_SCHEMA`; `node --check chatgpt/ir.mjs` | refactored |
| 3 | IR API | `chatgpt/ir.mjs`, callers | Renamed `materializeConversationIr` to `materializeThreadIr` and updated all callers. | grep gate: no `materializeConversationIr`; `node --check read-thread.mjs search-chatgpt.mjs test-cdp-scripts.mjs` | refactored |
| 4 | IR canonical data | `chatgpt/ir.mjs` | Removed legacy top-level `conversation` alias; canonical document now uses `thread` plus `_cdp` sidecar. | grep gate: no `ir.conversation`, `existing.conversation`, or `conversation:` legacy materialization | refactored |
| 5 | IR projections | `projectReadThreadResultFromIr`, `projectDownloadResolveFromIr`, search/project inventory projections | Removed `ir.thread || ir.conversation` fallbacks and made projections consume canonical `thread`. | `test-chatgpt-ir.mjs` projection assertions | refactored |
| 6 | adapter façade | `chatgpt.mjs` | Deleted top-level compatibility façade instead of preserving current-state compatibility. | file gate: `[ ! -e chatgpt.mjs ]`; grep gate: no `from "./chatgpt.mjs"` | refactored |
| 7 | adapter test | `test-chatgpt-adapter.mjs` | Replaced legacy façade test with aggregate `chatgpt/index.mjs` wiring test against responsibility modules. | `node --check test-chatgpt-adapter.mjs`; runtime gate: `qjs --std -m test-chatgpt-adapter.mjs` | refactored |
| 8 | aggregate exports | `chatgpt/index.mjs` | Kept one intentional aggregate export surface and added project-source helpers to it. | adapter wiring test checks exported bindings are identity-equal to responsibility modules | refactored |
| 9 | CDP core | `lib.mjs` | Merged required `chromium-cdp.lib.mjs` functionality into one canonical CDP core. | grep gate: no `from "./chromium-cdp.lib.mjs"`; `node --check lib.mjs test-cdp-error.mjs` | refactored |
| 10 | CDP errors | `CdpError`, `cdpError` | Moved structured CDP error contract into canonical `lib.mjs`. | `test-cdp-error.mjs`; docs updated to `lib.mjs` | refactored |
| 11 | CDP subprocess | `runToString`, `shToString` | Replaced qjs `os.exec` subprocess use with shell-quoted `std.popen` execution, preserving argv-style callers while avoiding qjs-ng exec hangs. | `node --check lib.mjs`; qjs runtime gates; callsites unchanged through exported function | refactored |
| 12 | preflight/login | `preflightCheck`, `detectLoginState`, `waitForLogin` | Preserved old library-only login/preflight functionality inside canonical `lib.mjs` before deleting old lib. | import grep gate; `node --check chromium-cdp-chatgpt-doctor.mjs chromium-cdp-chatgpt-login.mjs` | refactored |
| 13 | old dot implementation deletion | `chromium-cdp.*.mjs` legacy duplicates | Deleted old dot-prefixed duplicate implementations after moving their surviving functionality to canonical scripts/modules. | `find . -maxdepth 1 -type f -name 'chromium-cdp.*.mjs'` returns empty | refactored |
| 14 | read thread command | `read-thread.mjs` | Canonicalized imports and IR materialization to `materializeThreadIr`. | `node --check read-thread.mjs`; IR warm-path tests in `test-cdp-scripts.mjs` | refactored |
| 15 | send command | `send-chatgpt.mjs` | Kept canonical unprefixed implementation as the operational target for `chromium-cdp-send-chatgpt`. | Nix command map points `chromium-cdp-send-chatgpt -> send-chatgpt.mjs`; `node --check send-chatgpt.mjs` | refactored |
| 16 | upload command | `upload-chatgpt-file.mjs` | Kept canonical unprefixed implementation as the operational target for `chromium-cdp-upload-chatgpt-file`. | Nix command map points `chromium-cdp-upload-chatgpt-file -> upload-chatgpt-file.mjs`; `node --check upload-chatgpt-file.mjs` | refactored |
| 17 | search command | `search-chatgpt.mjs` | Updated warm-path cache/IR references to canonical thread IR. | `node --check search-chatgpt.mjs`; grep gate: no `existing.conversation` | refactored |
| 18 | hq threads | `hq-threads.mjs` | Kept canonical unprefixed implementation and removed old dot duplicate. | Nix command map points `chromium-cdp-hq-threads -> hq-threads.mjs`; `node --check hq-threads.mjs` | refactored |
| 19 | artifact download | `download-chatgpt-artifacts.mjs` | Kept canonical unprefixed artifact downloader and removed old dot duplicate. | Nix command map points `chromium-cdp-download-chatgpt-artifacts -> download-chatgpt-artifacts.mjs`; `node --check download-chatgpt-artifacts.mjs` | refactored |
| 20 | project inventory | `project-inventory.mjs` | Kept canonical unprefixed implementation and removed old dot duplicate. | Nix command map points `chromium-cdp-project-inventory -> project-inventory.mjs`; `node --check project-inventory.mjs` | refactored |
| 21 | projectize thread | `projectize-thread.mjs` | Kept canonical unprefixed implementation and removed old dot duplicate. | Nix command map points `chromium-cdp-projectize-thread -> projectize-thread.mjs`; `node --check projectize-thread.mjs` | refactored |
| 22 | project sources shared module | `chatgpt/project-sources.mjs` | Extracted shared Project Sources URL, target selection, navigation wait, and source-visible wait helpers. | `node --check chatgpt/project-sources.mjs chromium-cdp-upload-project-source-text.mjs chromium-cdp-upload-project-source-file.mjs` | refactored |
| 23 | project source text upload | `chromium-cdp-upload-project-source-text.mjs` | Replaced local Project Sources helper duplication with `chatgpt/project-sources.mjs`. | grep/diff gate: no local `sourcesUrl`/`pickTarget`; `node --check chromium-cdp-upload-project-source-text.mjs` | refactored |
| 24 | project source file upload | `chromium-cdp-upload-project-source-file.mjs` | Replaced local Project Sources helper duplication with `chatgpt/project-sources.mjs` while keeping filechooser binary-upload path. | grep/diff gate: shared `waitForProjectSourceVisibleExpr`; `node --check chromium-cdp-upload-project-source-file.mjs` | refactored |
| 25 | project source reread | `chromium-cdp-project-source-reread.mjs` | Changed subprocess target from deleted old dot implementation to canonical `send-chatgpt.mjs`. | grep gate: no `chromium-cdp.send-chatgpt.mjs`; `node --check chromium-cdp-project-source-reread.mjs` | refactored |
| 26 | project source workflows | `project-sources-*.mjs` | Kept canonical unprefixed workflow implementations and removed old dot duplicates. | Nix command map points to unprefixed workflow scripts; `node --check project-sources-*.mjs` | refactored |
| 27 | Nix command packaging | `chromium-cdp.nix` | Replaced repeated package/app wrappers with one `qjsCommands` map and `mkQjsCommand`. | static map gate: docs command names are present in `qjsCommands`; brace-count static check | refactored |
| 28 | operational entrypoints | `packages.<system>.cdp` | Retained `chromium-cdp-*` operational command names inside the single `cdp` package instead of deleting them or keeping individual outputs. | Nix map gate checks `chromium-cdp-*` names exist; no old dot script target remains | refactored |
| 29 | command-map docs | `docs/chatgpt-command-map.md`, `README.md`, `chatgpt/README.md` | Updated docs away from legacy façade/old library wording and toward canonical modules. | grep gate: no stale `chromium-cdp.lib.mjs` or façade claims in docs | refactored |
| 30 | CDP docs | `docs/cdp-errors.md`, `docs/cdp-preflight.md`, `docs/cdp-testing.md` | Updated documentation references to canonical `lib.mjs` and canonical script names. | grep gate over docs for deleted file names | refactored |
| 31 | old implementation references | all source/docs/Nix | Removed imports and command references to deleted old dot-prefixed files. | recursive grep gate for old dot-prefixed filenames returns empty | refactored |
| 32 | syntax gate | all retained `.mjs` files | Checked retained JavaScript module syntax after deletion/import rewiring. | `node --check` on retained `.mjs` groups | refactored |
| 33 | whitespace gate | full repo diff | Checked diff for whitespace errors before packaging. | `git diff --check` | refactored |
| 34 | artifact packaging | final repo | Produced a refactored zip and patch so the refactor can be applied/reviewed atomically. | `zip -r /mnt/data/cdp_latest_upstream_refactored_package_run.zip cdp`; `git diff upstream/latest..HEAD > /mnt/data/cdp_latest_upstream_package_run.patch` | refactored |
| 35 | package-run artifact contract | `cdp/package-run`, `chromium-cdp-package-run`, host-git orchestration | Fixed weak thread artifact materialization by declaring `patch` / `mbox` / `bundle` / `result.json` receive, validate, and apply steps as host orchestration responsibility, not thread-side free-form execution; added a thin command wrapper over the canonical `package-run.mjs` module. | `test-cdp-scripts.mjs` host artifact orchestration contract section; `test-package-run.sh` patch / mbox / bundle host-apply fixture; docs grep for `cdp/package-run`, `host orchestration`, `patch`/`mbox`/`bundle`/`result.json`. | refactored |
| 36 | package-run state contract | `chromium-cdp-package-run-state`, `run.json`, review quality gate | Kept run-level impl/review/finalize state separate from artifact application, so `chromium-cdp-package-run` can stay a host artifact applier while `chromium-cdp-package-run-state` owns `run.json` and canon-tdd review quality checks. | `test-cdp-scripts.mjs` package-run state machine test; docs grep for `package-run state contract`; help gate for `chromium-cdp-package-run-state`. | refactored |

## Environment-limited gates

This pass executed Node syntax gates, qjs unit/warm-path gates, package-run host artifact gates, and Nix/static command-map checks where available. A live Chromium CDP endpoint was not available in this environment, so live browser smoke remains an external gate.
