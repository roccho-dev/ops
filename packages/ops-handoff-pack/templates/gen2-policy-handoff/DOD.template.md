# Definition of Done Template

Done only when:

- Gen2 first readback cites contextPacketId and fixed policyEntryRef digest.
- Gen2 reads required source files or returns blocker.
- Worker output includes concrete diff/proof or no-change report.
- Reviewer output is PASS/BLOCK with evidence refs.
- No semantic/completion/canonical approval is claimed by transport or worker.
- Gen1 reviews Gen2 output before anything is reported upward to Gen0.
