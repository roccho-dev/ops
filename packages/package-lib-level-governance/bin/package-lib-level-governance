#!/usr/bin/env sh
set -eu
HERE=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PKG=$(CDPATH= cd -- "$HERE/.." && pwd)
export PYTHONPATH="$PKG/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m package_lib_level_governance "$@"
