# Sample handoff request

Improve the supplied candidate while obeying role separation.

The first proof is local-only. Do not call CDP, upload Project Source files,
merge, push, or claim semantic approval.
