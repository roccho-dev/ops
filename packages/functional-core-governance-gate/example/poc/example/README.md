# functional-core-governance-gate example

This example is explicit fixture/demo material only. It is not scanned by default
and is not authority. The manifest classifies `core/decider.py` as functional
core and keeps file I/O in `adapter/runner.py`.
