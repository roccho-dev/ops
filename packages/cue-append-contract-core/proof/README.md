# Proof area

This directory contains proof runners, fixtures, receipts, and scale experiments.
Python helpers live here so they cannot be mistaken for core implementation.
Core is Go + CUE + JSONL; Python is proof/helper infrastructure unless promoted
behind an explicit adapter boundary.
