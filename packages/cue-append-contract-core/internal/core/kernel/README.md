# Legacy kernel package

P13 removed the Go implementation from this facade. Core behavior is split across `internal/core/validate`, `artifacts`, `admission`, `authority`, `receipt`, `graph`, `sourcepolicy`, `lineage`, and `partition`.
