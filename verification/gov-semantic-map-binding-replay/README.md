# Governance package Semantic Map binding replay

Bounded provider proof for `roccho-dev/ops#363`.

```text
exact governance packages.jsonl
+ exact merged UI profile and Semantic Map generator
→ runtime binding only
→ existing gov-release-proxy Worker
→ same-root HTML / NDJSON
→ local and remote Chromium
```

The generated HTML and receipts are evidence only. Governance owns meaning, UI owns projection/rendering, and Ops owns delivery/readback. `authority=false` and `productionCutover=false`.
