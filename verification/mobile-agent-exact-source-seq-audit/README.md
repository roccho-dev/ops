# Exact-source Seq public URL audit

This path fixes the existing Mobile Agent source commit/tree, runs only its existing verification and build, publishes the resulting exact 54-file dist as an immutable Carrier, deploys the same bytes to `stg-mobile-agent`, and reports `seq/1` only from the canonical PASS receipt after stable and deployment-specific real-Chrome proof.

No new preset, renderer, layout, interaction shell, codec, backend, database, or `example/*.html` substitute is introduced.
