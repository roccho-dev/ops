PEER_REVIEW 1. Strongest points from B  B’s strongest contribution is the invariant-vs-HOWTO distinction. I agree that global policy should define authority, provenance, replayability, validation boundaries, archive integrity, approval boundaries, and backend declarations, while repo-local design should own concrete storage choices, compression mechanics, SQLite/materialized views, and runtime adapters. That sharpens A’s “minimal interface, repo-local richer schema” position rather than replacing it.   THREAD_B_REPLY_V1  B also adds a needed concurrency dimension. A focused on bloat, legacy glob failure, evidence refs, and validation modes; B correctly says Git-managed issues are only safe if the repo declares whether it is single-writer, multi-writer, or hybrid. That is a real blind spot in A: issues/*.jsonl can be append-only and still suffer same-issue contention around status, ownership, dependencies, closure, or blocker fields.  B’s approval-boundary point is important and should be adopted. File upload, readback, thread reply, generated report, branch inventory, or issue-ledger record should not by themselves authorize merge, push, cleanup, branch deletion, issue closure, or implementation acceptance. This aligns with the Project Source rule that facilitator summary is not a verdict and same-proposalRevision no-objections are required before pass.   ISSUE_LEDGER_POLICY_DISCUSSION_…  B’s event/replay/snapshot/archive framing is a useful escalation path for large volume. The current policy snapshot says issue queues use JSONL, each line is a complete issue.record.v1 snapshot, later lines update by repeating issueId with supersedes, and validation currently targets the complete ledger set because supersedes must resolve.   ISSUE_LEDGER_POLICY_DISCUSSION_…   B is right that, at scale, raw log scans should not be the normal read path; there should be a replayable canonical source plus derived latest-state views, snapshots, segment manifests, and cold archive verification.  B’s generated-view point should be accepted. Markdown issue pages, dashboards, summaries, and review packets should be disposable views, not canonical issue state. That reinforces A’s concern that Markdown/evidence directories under ops/issues/ blur machine authority and legacy evidence. The Project Source notes that Markdown issue files are legacy evidence by policy, while repo reality still includes Markdown and evidence directories under ops/issues/.   ISSUE_LEDGER_POLICY_DISCUSSION_…  B’s post-publish divergence risk is worth adding. A emphasized validation false positives and false negatives. B adds the concrete risk that code state and issue state can diverge after publish unless remote state is compared. Even if refs/issues are not the current canonical shape, the agenda item should generalize to “atomicity or post-publish verification for code + issue state.”  2. Objections to B  First, B did not fully satisfy the original round-1 shape. The Project Source required agenda enumeration and classification across the named topics, including Git-managed issue location, evidence boundaries, append-only volume, compression/archive, legacy JSONL, validation modes, repo-specific schema, blocked, narrowing, split-vs-supersede, structured evidence refs, legacy migration, and worker-start invariant.   ISSUE_LEDGER_POLICY_DISCUSSION_…   B’s agenda is a compact paragraph, not a numbered classified agenda. That is a format and process gap, even though its substance is useful.  Second, B’s opening source-boundary caveat weakens its authority. B says it treated ISSUE_LEDGER_POLICY_DISCUSSION_V1.md as controlling but did not retrieve the exact verbatim file.   THREAD_B_REPLY_V1   In this peer review, the controlling file is available and shows concrete current-policy and ops-repo facts, including 260519.jsonl legacy shape, full gate failure, scoped current-v1 success, the narrowed Project Source issue, the NO_UNRESOLVED_OBJECTIONS marker bug, and mixed Markdown/evidence directories.   ISSUE_LEDGER_POLICY_DISCUSSION_…   B’s architectural objections should be retained, but its review should be considered revision input, not a directly sufficient peer signoff.  Third, B may overcorrect from “Git issue ledger has gaps” to “event-sourced platform redesign.” The current policy is snapshot-based: each JSONL line is a complete issue.record.v1, with updates via issueId and supersedes.   ISSUE_LEDGER_POLICY_DISCUSSION_…   B’s proposed event schema, replay layer, source-artifact ledger, snapshots, cold archive lifecycle, fsck suite, and generated outputs may be right for scaled repos, but they are not a minimal patch. This is a repo-local design track plus possibly a later policy track, not something to fold wholesale into v1.  Fourth, B under-addresses several immediate A/source issues: legacy JSONL quarantine, active/archive/legacy path or manifest, scoped active-chain validation, blocked scope, issue narrowing, split-vs-supersede criteria, and the current ops issues. A specifically proposed a control-plane/data-plane split, active/archive/legacy validation classes, and structured evidenceRefs[], while preserving richer repo-local schema.   THREAD_A_REPLY_V1   B’s event model should not displace those immediate fixes.  Fifth, B’s evidence-state taxonomy is directionally correct but may be too broad for the issue-ledger policy itself. raw_evidence, candidate_summary, adopted_policy, rejected, deprecated, hold, and out_of_scope are useful, but some belong to Project Source artifact governance or discussion policy rather than every issue record. The policy patch should require source/evidence classification hooks; repo-local design can choose the full taxonomy.  Sixth, B relies on adjacent artifacts such as ticket-vs-Beads, JSONL+SQLite PoC, snapshot lifecycle, runtime boundary, and worst-case simulations. Those are useful supporting evidence, but the peer-review decision for this discussion should distinguish “direct Project Source facts” from “adjacent design evidence.” Otherwise policy can be captured by implementation-specific examples.  3. Agenda items to add/remove/rename  Add:  Issue backend/concurrency mode — policy-rule-change candidate. Each repo should declare single-writer, multi-writer, or hybrid semantics, including CAS/claim rules for status, ownership, dependency, blocker, and closure fields.  Event stream vs complete snapshot model — policy-rule-change candidate. The current policy is complete issue.record.v1 snapshots with supersedes; B proposes event streams. The agenda must decide whether v1 stays snapshot-based, moves event-based, or permits both behind a minimal interface.  Approval boundary — policy-rule-change candidate. Explicitly state that artifact existence, issue records, generated reports, summaries, and readbacks are evidence, not approval.  Generated views are non-authoritative — repo-local issue design change candidate. Markdown pages, dashboards, latest SQLite, and review packets must be rebuildable and disposable unless explicitly promoted.  Latest-state materialization threshold — repo-local issue design/tooling-gate task. Define when a repo may or must stop scanning raw JSONL for ordinary reads and use a derived latest-state view.  Replay/fsck/idempotence requirements — tooling/gate task. Add deterministic replay, duplicate-event/idempotency behavior, missing-parent/supersedes checks, evidence hash checks, and generated-view rebuild checks.  Snapshot/segment/cold archive lifecycle — evidence storage/retention task. Compression should be governed by manifest, hash, restore, and replay boundaries, not merely file size.  Code-state/issue-state publication consistency — tooling/gate task. Decide whether atomic publication is required or whether post-push compare-and-repair is acceptable.  Source/evidence state taxonomy — policy-rule-change candidate, but with scope control. Require classification; do not necessarily mandate B’s full vocabulary globally.  Policy-vs-runbook boundary — policy-rule-change candidate. Prevent global policy from absorbing package, WebMCP, CDP, forge, SQLite, or shell HOWTO details.  Rename:  Rename “Should issues live inside Git-managed repo paths at all?” to “Should Git hold the issue control plane, the evidence data plane, or both?”  Rename “Does append-only JSONL make repo volume grow too much?” to “How do append-only audit logs combine with materialized latest views, snapshots, and cold archive?”  Rename “If compression/archive exists, does it remove the concern?” to “What archive lifecycle preserves issue meaning, evidence integrity, and replay?”  Rename “Should validation cover all issues/*.jsonl?” to “What validation mode applies: active, full-v1/full-replay, migration, release, or archive-restore?”  Rename “Should repo-specific issue design be allowed?” to “What global issue interface must repo-local backends project to?”  Rename “How should blocked be defined?” to “What is the scope and authority boundary of issue status values?”  Rename “When use same issueId/supersedes vs split?” to “What are the mutation semantics for narrowing, supersession, split, reopen, and event/snapshot transition?”  Remove or demote:  Do not remove any of the original fifteen required agenda topics. Demote implementation-specific examples—SQLite, Beads/Dolt, refs/issues, gzip/zstd, WebMCP, package-specific commands—from global policy agenda to repo-local design evidence unless the policy text only uses them as non-normative examples.  4. Disposition of v1  v1 should not become a direct implementation-ready policy patch. It should become a split work package:  Policy patch seed: control-plane/data-plane split; minimal issue interface; structured evidence/source refs; evidence classification; approval boundary; schema/path/manifest declaration; validation modes; blocked scope; split/narrow/supersede rules; archive integrity invariants; backend mode declaration.  Repo-local design task: choose ops issue backend mode; define active/archive/legacy paths or manifest; decide whether to remain snapshot JSONL or add event-stream projection; define latest-state materialization; define generated views; define archive storage; define post-publish verification.  Migration task: quarantine or manifest-exclude 260519.jsonl; preserve it as legacy evidence; keep the passing 260524 + 260525 + 260526 current-v1 chain intact; inventory Markdown/evidence directories; add migration/index records without rewriting legacy history. The source shows the legacy file fails the full glob gate while the scoped current-v1 chain passes.   ISSUE_LEDGER_POLICY_DISCUSSION_…  Discussion-only for now: event-sourced canonical model, SQLite/latest-state thresholds, cold archive restore SLA, same-issue multi-agent mutation semantics, and atomic issue/code publication. These need further decision before policy wording becomes mandatory.  So the disposition is: revise-required, not pass; advance to policy patch seed + repo-local design + migration tasks, with some B-level architecture items still discussion-only.  5. Unresolved objections  Snapshot vs event model remains unresolved. Current policy says complete issue.record.v1 snapshots with supersedes; B proposes event records with replay. That is a major semantic fork, not just an implementation detail.  B must redo or supplement its review against the directly available Project Source. Its source-boundary caveat is honest, but a v1-peer review should not rest on indirect summaries when the file is available.  Agenda compliance remains unresolved. B’s review should be normalized into the required numbered/classified agenda before it can support no-objections.  Global policy scope remains too fuzzy. Approval boundary and validation modes belong in policy; SQLite, specific repo layout, Beads/Dolt comparisons, and package runbooks likely do not.  Legacy JSONL handling is still a blocker. A’s quarantine/manifest approach is concrete; B’s broader migration-to-events does not yet answer the immediate 260519.jsonl false-failing gate.  Large evidence handling needs exact recovery semantics. Hashes and cold archive are not enough; policy must say what a fresh clone without artifact credentials can list, verify, or reproduce.  Concurrency mode needs a default. I would default to hybrid: append-only comments/evidence can be multi-writer, but status, ownership, dependency, blocker, closure, and supersession require claim/CAS or single-writer control.  Publication consistency needs a non-optional check. B is right to raise code/issue divergence, but policy must decide whether this is release-gate mandatory or repo-local tooling guidance.  blocked, narrowing, and split-vs-supersede still need exact text. B’s architecture does not replace A’s need for worker-facing status semantics.  No approval state should be inferred from this exchange. The Project Source explicitly says same-proposalRevision no-objections are required before pass, and this peer review still records unresolved objections.   ISSUE_LEDGER_POLICY_DISCUSSION_…
