# governance records fixture

This directory is a CI fixture for `ops` flake checks.

The rows under `records/` are attached fixture data used only to let GitHub
Actions evaluate the existing `prove-feat` checks without tailnet-local
governance records. They are not accepted governance authority.

