# Branch status

This branch is a candidate only. Merge and downstream cutover remain blocked until the same-head exact Nix Green evidence exists and the edits strict shadow adapter consumes the resulting bytes.
