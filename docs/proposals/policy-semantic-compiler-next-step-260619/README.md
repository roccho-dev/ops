# Policy Semantic Compiler Next Step

Status: proposal only.

This proposal records Codex readback of the ChatGPT proposal
`policy-semantic-authority-closure-260619`. It does not implement the compiler
yet, does not approve cutover, and does not delete policy.git.

## Review Decision

Adopt the sequencing direction with corrections. The next ops candidate should
be a pinned DuckDB-based compiler and falsification harness that proves semantic
graph inputs can produce native policy/role records and fail closed under
counterexamples.

## Proposed Package

`packages/policy-semantic-compiler/`

| Path | Purpose |
|---|---|
| `sql/integrity.sql` | validate source/span/semantic/projection graph integrity |
| `sql/compile.sql` | emit native policy and role records |
| `sql/gates.sql` | compute cutover gate status and fail-closed blockers |
| `tests/edge-counterexamples.jsonl` | independently authored negative cases |
| `tests/fresh-agent-cases.jsonl` | immutable-input fresh-agent decision cases |
| `tests/run.sh` | clean two-run compiler and byte reproducibility gate |
| `README.md` | authority boundary and operation notes |

## Required Corrections

- The host/reviewer verified ZIP baseline is
  `e5c100ae58fcb38f6ef6574df3c58427751cddf8fb6ba68ba37e41500740e03c`.
  The worker-thread displayed `7ac324...` hash is not accepted evidence.
- Python may orchestrate, but the canonical projection and gates must execute in
  DuckDB. Python-equivalent logic must leave the DuckDB gate failed.
- Reproducibility checks must compare full generated output trees from two
  clean runs. Differences must be fixed at the source; do not strip divergent
  fields after generation.
- `index.html` and `strict-field-result.json` are historical mismatch evidence.
  They become required outputs only if the accepted governance projection
  contract names them.

## Required Counterexamples

- Missing or shifted source span.
- MUST weakened to advisory or permission.
- deny changed to allow.
- missing activation edge.
- role widened to wildcard scope.
- exit edge skipping required review.
- native row emitted without projection edge.
- consumer edge marked migrated without consumer diff.
- stale direct `policy.git` reference claimed migrated.
- input row reorder changes generated bytes.
- Python-only run claims DuckDB gate pass.
- source-tree byte parity exists while graph records are deleted.

## Completion Vocabulary

Allowed candidate claim: `semantic-authority-closure-ready-for-review`.

Forbidden claims:

- `cutover-ready`
- `policy.git may be deleted`
- `policy logic deleted`
- `semantic approval granted`

